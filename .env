############################################
# SMART on FHIR Client Configuration (.env)
############################################

# Server port the SMART client listens on
SMARTCLIENT_PORT=3005

# SMART client credentials/config
SMART_CLIENT_ID=simpleclient_xwxa

# Default FHIR base (iss) for standalone launches (optional)
# If launching from an EHR, it will provide iss and this is ignored.
# Leave unset in production to require iss; set only for local testing.
# DEFAULT_FHIR_BASE=https://dev.corisystem.org/smart/fhir

# Redirect URI registered for the client
SMART_REDIRECT_URI=http://localhost:3005/callback

# Optional: OIDC client secret (typically not used for public clients)
# CLIENT_SECRET=blahblah

# --- Local development example ---
# To target a local CORI instance for standalone testing, use:
# DEFAULT_FHIR_BASE=http://localhost:3001/smart/fhir

# Optional: Node environment
# NODE_ENV=development


